import 'package:flutter/material.dart';

class settingsFragment extends StatelessWidget {
  const settingsFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is my settingsFragment"),
      ),
    );
  }
}
